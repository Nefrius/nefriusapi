<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Paneli - Dosya Yükleme</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1 class="my-4">Admin Paneli - Dosya Yükleme</h1>

    <!-- Dosya yükleme formu -->
    <form action="/upload" method="POST" enctype="multipart/form-data" class="mb-4">
      <div class="mb-3">
        <label for="file" class="form-label">Dosya Seç</label>
        <input type="file" class="form-control" id="file" name="file" required>
      </div>
      <button type="submit" class="btn btn-primary">Yükle</button>
    </form>

    <!-- Yüklenen dosyalar listesi -->
    <h2>Yüklenen Dosyalar</h2>
    <ul class="list-group">
      <% if (files.length > 0) { %>
        <% files.forEach(function(file) { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <a href="/uploads/<%= file.name %>" target="_blank"><%= file.name %></a>
            <form action="/admin/delete/<%= file.name %>" method="POST" style="display:inline;">
              <button type="submit" class="btn btn-danger btn-sm">Sil</button>
            </form>
          </li>
        <% }); %>
      <% } else { %>
        <li class="list-group-item">Henüz yüklenmiş dosya yok.</li>
      <% } %>
    </ul>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
